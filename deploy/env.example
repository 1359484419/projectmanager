# /opt/pm/env 模板（服务器上真实文件权限必须 600，绝不提交真实值）
DB_URL=jdbc:postgresql://127.0.0.1:5432/pm
DB_USER=pm
DB_PASS=<数据库密码，openssl rand -hex 16 生成>
JWT_SECRET=<openssl rand -hex 32 生成，HS256 至少 32 字节>
# 对外访问地址（邀请链接等对外 URL 的前缀），不设则默认 http://localhost:5173
PM_BASE_URL=http://<公网IP或域名>:8080
